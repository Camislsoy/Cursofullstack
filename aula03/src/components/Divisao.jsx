import React from 'react'

const Divisao = (props) => {
  return (
    <div>
        O resultado da divisao é {props.num1} / {props.num2} = {props.num1/props.num2}
    </div>
  )
}

export default Divisao